#include "StdAfx.h"
#include "MathHelper.h"



/* m_digest_edit.Empty();
    CString tmp;
    for (int i = 0; i<16; i++)
    {
         tmp.Format("%02x", bHash[i]);
         m_digest_edit+=tmp;
     }

     CryptDestroyHash(hHash); 
     CryptReleaseContext(hCryptProv, 0); 
*/